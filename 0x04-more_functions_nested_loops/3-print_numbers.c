#include "main.h"
/**
 * print_numbers - check description
 * Description: prints numbers 0-9
 * Return: Always (0)
 */

void print_numbers(void)
{
int i;
if (i >= '0' && i <= '9')
{
_putchar(i, '\n')
}

return (0);
}

